name: Cypress test

on: [push]

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install Dependencies
        run: npm ci
      - name: Cypress run
        uses: cypress/base@1.7.2
        with:
          start: npm start
